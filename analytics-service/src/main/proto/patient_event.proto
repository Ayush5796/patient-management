syntax = "proto3";

package patient.events;
option java_multiple_files = true;    //Generate Separate file instead of
                                      // having all of the classes generated under the single parent class

message PatientEvent {
  string patientId = 1;
  string name = 2;
  string email = 3;
  string event_type=4;
}